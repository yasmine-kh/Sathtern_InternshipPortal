/**
 * Default (production) environment.
 * Replaced by environment.development.ts for the development configuration
 * via the fileReplacements entry in angular.json.
 */
export const environment = {
  production: true,
  /** Backend API base URL. Set to the deployed API before shipping. */
  apiBaseUrl: '/api',
};
